# Settings page styling update

The Settings page was restyled without changing its existing settings behavior.

- Added a polished Settings header card.
- Grouped controls into Appearance and Preferences sections.
- Added icon tiles, descriptions, status pill, spacing, borders, and typography.
- Improved dark/light theme contrast.
- Added pressed-state feedback to rows that already support actions.
- Added a subtle footer for a cleaner finish.
