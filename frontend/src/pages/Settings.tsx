import { Bell, ChevronRight, Lock, Moon, Palette, ShieldCheck, Sun } from "lucide-react"; import { ThemeToggle } from "@/components/ui/ThemeToggle"; import { useTheme } from "@/lib/theme";
export default function Settings(){const {theme}=useTheme();return <div className="mx-auto w-full max-w-2xl space-y-6"><header><p className="text-xs font-semibold uppercase tracking-[0.18em] text-accent">Preferences</p><h1 className="mt-1 font-display text-3xl text-ink">Settings</h1></header><section className="overflow-hidden rounded-2xl border border-border bg-surface"><div className="flex items-center gap-3 border-b border-border p-4"><span className="flex h-9 w-9 items-center justify-center rounded-lg bg-accent-soft text-accent-strong"><Palette className="h-4 w-4"/></span><div className="min-w-0 flex-1"><p className="text-sm font-semibold text-ink">Appearance</p><p className="text-xs text-ink-faint">Choose how Marketplace looks on your device.</p></div><ThemeToggle/></div><div className="flex items-center gap-3 border-b border-border p-4"><span className="flex h-9 w-9 items-center justify-center rounded-lg bg-surface-sunken text-ink-soft">{theme==="dark"?<Moon className="h-4 w-4"/>:<Sun className="h-4 w-4"/>}</span><div><p className="text-sm font-semibold text-ink">{theme==="dark"?"Dark mode":"Light mode"}</p><p className="text-xs text-ink-faint">Your preference is saved automatically.</p></div></div><Row icon={Bell} title="Notifications" subtitle="Choose what you want to hear about"/><Row icon={Lock} title="Password & security" subtitle="Manage account security"/><Row icon={ShieldCheck} title="Privacy" subtitle="Control your personal data"/></section></div>}
function Row({icon:Icon,title,subtitle}:{icon:typeof Bell;title:string;subtitle:string}){return <button className="flex w-full items-center gap-3 border-b border-border p-4 text-left last:border-b-0 hover:bg-surface-sunken"><span className="flex h-9 w-9 items-center justify-center rounded-lg bg-surface-sunken text-ink-soft"><Icon className="h-4 w-4"/></span><span className="min-w-0 flex-1"><span className="block text-sm font-semibold text-ink">{title}</span><span className="block text-xs text-ink-faint">{subtitle}</span></span><ChevronRight className="h-4 w-4 text-ink-faint"/></button>}
