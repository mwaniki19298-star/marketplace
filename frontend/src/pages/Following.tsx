import { Heart } from "lucide-react"; import { StoreCard } from "@/components/ui/StoreCard"; import { mockStores } from "@/data/mock";
export default function Following(){const stores=mockStores.filter(s=>s.isFollowing);return <div className="space-y-6"><header><p className="text-xs font-semibold uppercase tracking-[0.18em] text-accent">Your community</p><h1 className="mt-1 font-display text-3xl text-ink">Following</h1><p className="mt-1 text-sm text-ink-faint">Stores you follow will appear here.</p></header>{stores.length?<div className="flex gap-3 overflow-x-auto pb-2">{stores.map(s=><StoreCard key={s.id} store={s}/>)}</div>:<div className="rounded-2xl border border-dashed border-border-strong p-10 text-center"><Heart className="mx-auto h-8 w-8 text-ink-faint"/><p className="mt-3 font-semibold text-ink">No followed stores yet</p></div>}</div>}
